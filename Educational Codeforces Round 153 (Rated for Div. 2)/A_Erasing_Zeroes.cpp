#include <bits/stdc++.h>
using namespace std;
int main()
{
    // if the string is 0, 1, 00111 or 01111100
    int t;
    cin >> t;
    while (t--)
    {
        string s;
        cin >> s;
        string carry_s = s.substr(0, 3);
        // cout << carry_s << endl;
        if (carry_s == 0)
        {
        }
    }

    return 0;
}