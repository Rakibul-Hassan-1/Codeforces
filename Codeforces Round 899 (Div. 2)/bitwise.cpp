#include <bits/stdc++.h>
using namespace std;
int main()
{

    int n;
    cin >> n;
    // left shif oparator which one exact do the similer of 2xN
    cout << (n << 1) << " " << (n << 2) << " " << (n << 3);

    return 0;
}